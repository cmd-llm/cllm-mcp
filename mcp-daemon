#!/bin/bash
# MCP Daemon control script
# Simple wrapper for mcp_daemon.py

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec python3 "$SCRIPT_DIR/mcp_daemon.py" "$@"
